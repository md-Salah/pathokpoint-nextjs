import { TopBanner, SecondNav } from "@/components";
import Nav from "./Nav";

const Navbar = () => {
  return (
    <div>
      <TopBanner />
      <Nav />
      <SecondNav />
    </div>
  );
};

export default Navbar;
