import { BookCarousel, HeroSection } from "@/components";

const Home = () => {
  return (
    <div className="min-h-screen pb-36">
      <HeroSection />
      <BookCarousel />
    </div>
  );
};

export default Home;
